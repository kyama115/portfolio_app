<% shops.each do |shop| %>
  <div class="p-3 hover:bg-gray-50 cursor-pointer"
       data-action="click->autocomplete#select"
       data-value="<%= shop.title %>">
    <div class="text-sm">
      <%= shop.title %>
    </div>
  </div>
<% end %>